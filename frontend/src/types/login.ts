export type loginCredentials = {
   clientId:string,
   select_by:string,
   credential:string
}

export type userDataType = {
   name:string,
   email:string,
   picture:string,
   savedFiles:string[]
}