

const StandardHeading = ({title}:{title:string}) => {
  return (
    <div style={{letterSpacing:'2px'}} className="standard-heading text-primary text-4xl text-center mt-5 font-bold mb-3 w-[97vw]">{title}</div>
  )
}

export default StandardHeading